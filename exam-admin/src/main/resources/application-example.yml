# ============================================================
# 部署配置模板：复制为 application-prod.yml 或通过环境变量注入
# cp application-example.yml application-prod.yml
# ============================================================
spring:
  datasource:
    url: jdbc:mysql://DB_HOST:3306/campus_exam?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai
    username: EXAM_APP
    password: CHANGE_ME_STRONG_PASSWORD
  data:
    redis:
      host: REDIS_HOST
      port: 6379
      password: CHANGE_ME
  rabbitmq:
    host: MQ_HOST
    username: exam
    password: CHANGE_ME

exam:
  jwt:
    secret: CHANGE_ME_TO_A_RANDOM_32_BYTES_SECRET
